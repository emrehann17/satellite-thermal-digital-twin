# Window-closure regional implementation

This historical package path contains the shared implementation used by the
independent `window-closure-region` command. Production identity, namespace,
resume state, manifest, and validation are scoped to exactly one AOI.

There is no combined multi-AOI command, execution namespace, validator, or
fallback alias. Cross-AOI synthesis is outside the production dependency graph.
