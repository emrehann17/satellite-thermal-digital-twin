# Advisor final numerical package

Start with `ADVISOR_NUMERICAL_SUMMARY.md` and `EMAIL_READY_NUMBERS.md`. Exact machine-readable values are in `ALL_NUMBERS.json` and `tables/`; provenance is in `SOURCE_INDEX.csv` and `GIT_AND_EXPORT_PROVENANCE.md`. All unverified fields use the literal token `NOT_VERIFIED_FROM_FINAL_ARTIFACT`.
