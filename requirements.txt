earthengine-api>=1.7,<2
geemap>=0.37,<1
gdown>=6,<7
matplotlib>=3.10,<4
numpy>=2.4,<3
pandas>=3.0,<4
python-dotenv>=1.2,<2
rasterio>=1.5,<2
scikit-learn>=1.9,<2
pyarrow>=24,<25
openpyxl>=3.1,<4

# Optional dependency.
# Required only when explicitly using --model xgboost in Step7C/Step8B.
# The default random_forest / hist_gradient_boosting workflows do not require it.
# Step8D supports only random_forest / hist_gradient_boosting.
#
# Install separately when needed:
#   pip install "xgboost>=3.3,<4"